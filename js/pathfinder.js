var button = document.getElementById('OkButton');

button.addEventListener('click', function(){
    var rows = document.getElementById('RowInput');
    var columns = document.getElementById('ColumnInput');

    console.log(rows);
    console.log(columns);
});